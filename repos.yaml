repos: 
  cineca_builtin_default: ${SPACK_DEPLOY}/repos/cineca_$SPACK_REPO_DEFAULT_VERSION
  cineca_new: ${SPACK_DEPLOY}/repos/cineca_new
  builtin_default: ${SPACK_DEPLOY}/repos/$SPACK_REPO_DEFAULT_VERSION/repos/spack_repo/builtin


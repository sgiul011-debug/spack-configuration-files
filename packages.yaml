packages:
  all:
    variants:
    - cuda_arch=90
    - +cuda
  cuda:
    require:
    - "@13.0.2"
    - +compat
    - +cupti
    - +samples~full 
    - samples_arch=90
  gcc: 
    spec: gcc@15.2.0 -bootstrap +nvptx
    
    
